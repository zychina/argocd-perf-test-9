apiVersion: v1
kind: Service
metadata:
  name: perf-test9-app97-svc
  namespace: clawtest
  labels:
    app: perf-test9-app97
spec:
  selector:
    app: perf-test9-app97
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
