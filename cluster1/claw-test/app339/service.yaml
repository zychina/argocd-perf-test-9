apiVersion: v1
kind: Service
metadata:
  name: perf-test9-app339-svc
  namespace: clawtest
  labels:
    app: perf-test9-app339
spec:
  selector:
    app: perf-test9-app339
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
